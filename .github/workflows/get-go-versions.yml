name: Get Go versions
on:
  schedule:
    - cron: '0 0,12 * * *'
  workflow_dispatch:

jobs:
  get-new-go-versions:
    uses: actions/versions-package-tools/.github/workflows/get-new-tool-versions.yml@dependabot/github_actions/actions/download-artifact-8    
    with:
      tool-name: "Go"
      image-url: "https://go.dev/images/gopher-footer.jpg"
    secrets: inherit
