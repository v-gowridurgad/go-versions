name: Create Pull Request
on:
  workflow_dispatch:

jobs:
  create-pr:
    uses: actions/versions-package-tools/.github/workflows/create-pr-to-update-manifest.yml@dependabot/github_actions/actions/download-artifact-8    
    with:
      tool-name: "go"
    secrets: inherit